coverage:
  status:
    patch:
      default:
        target: 80%
    project:
      default:
        target: 90
        threshold: 1%
      CPU:
        paths:
          - src/6502
      PPU:
        paths:
          - src/ppu
      cartridge:
        paths:
          - src/cartridge
      bus:
        paths:
          - src/bus
